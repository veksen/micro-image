Before writing any code for this task:

1. **Research:** Read relevant source files, check library docs if needed, check `BUGS.md` and
   `.claude/rules/mistakes.md` for past lessons. Summarize findings.
2. **Plan:** State what will change, which files, the approach, risks, and what tests are
   needed. If multiple approaches exist, list trade-offs.
3. **Check the open decisions:** If the plan assumes an answer to a proposed ADR in
   `docs/adr/`, say which — and resolve that ADR rather than settling it implicitly in code.
4. **Wait:** Present the plan and WAIT for confirmation. Do NOT write code yet.

Task: $ARGUMENTS
